// import dateToday from "./dateToday";
// import formattedDate from "./formattedDate";

// const filterToday = (events, dateOption) => {
//     if (dateOption === "today") {
//         return events.filter(
//             (event) =>
//                 dateToday() === formattedDate(event.dates.start.localDate)
//         );
//     }
//     return events;
// };

// export default filterToday;
